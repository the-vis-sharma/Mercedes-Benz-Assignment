# Kafka With Zookeeper Docker

### Description
This contains a docker-compose.yml file to create and run docker image of Kafka with Zookeeper. This will need docker 
installed in system.

### Step to Run
```shell
docker-compose up -d
```